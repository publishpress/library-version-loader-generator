# Codeception Test Suite Configuration
#
# Suite for unit tests not relying WordPress code.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - Filesystem
    step_decorators: ~        
